{
  "name": "project-mean-backend",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "bcryptjs": "^2.0.1",
    "body-parser": "^1.19.0",
    "dotenv": "^10.0.0",
    "express": "^4.16.3",
    "jsonwebtoken": "^8.2.1",
    "mongoose": "^6.0.3",
    "mongoose-unique-validator": "^2.0.1",
    "multer": "^1.3.0"
  },
  "scripts": {
    "build": "ninja -v & ls -l && ls -Rl backend/",
    "start": "node server.js",
    "clean": "ninja -v -t clean & ls -l && ls -Rl backend/"
  }
}
